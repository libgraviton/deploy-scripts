# this file contains the main configuration for the library
deploy-scripts:
  cf_bin: cf
  cf_api_url: API_URL
  cf_username: Jon Doe
  cf_password: my_secret
  cf_org: ORG
  cf_space: SPACE
  cf_domain: DOMAIN
  cf_services:
    mongodb: free
